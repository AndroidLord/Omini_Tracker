package com.example.ominitracker.ui.screen.onboarding

/*@Composable
fun OnboardingPagerContent(
    modifier: Modifier = Modifier,
    pagerState: PagerState,
    userInfo: UserInfo,
    unlockedPages: Int,
    onUserInfoChange: (UserInfo) -> Unit
) {
    HorizontalPager(
        state = pagerState,
        modifier = modifier,
        userScrollEnabled = pagerState.currentPage < unlockedPages
    ) { page ->
        when (OnboardingPage.allPages[page]) {
            OnboardingPage.Name -> NameInputScreen(
                name = userInfo.name,
                onNameInputChange = { onUserInfoChange(userInfo.copy(name = it)) }
            )
            OnboardingPage.Age -> AgeInputScreen(
                age = userInfo.age?.toString() ?: "",
                onAgeChange = { onUserInfoChange(userInfo.copy(age = it)) }
            )
            OnboardingPage.Gender -> GenderInputScreen(
                gender = userInfo.gender,
                onGenderInfoChange =  { onUserInfoChange(userInfo.copy(gender = it)) }
            )
        }
    }
}*/

